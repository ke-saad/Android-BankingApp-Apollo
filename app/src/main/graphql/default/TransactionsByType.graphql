query GetTransactionsByType($type: TypeTransaction!) {
  transactionsByType(type: $type) {
    id
    montant
    date
    type
    compte {
      id
      solde
      dateCreation
      type
    }
  }
}
