query GetTransactionsByTypeAndAccountId($type: TypeTransaction!, $accountId: ID!) {
  allTransactionsByTypeAndAccountId(type: $type, accountId: $accountId) {
    id
    montant
    date
    type
    compte {
      id
      solde
      dateCreation
      type
    }
  }
}
